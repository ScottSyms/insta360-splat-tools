// Temporal shadow correlation — stub for §5 temporal movement correlation.
// Future: compare shadow mask across N keyframes, keep only moving-dark regions correlated with person motion.

pub fn temporal_filter(_masks: &[Vec<u8>]) -> Vec<u8> {
    vec![]
}
